# Ping Scanner - Асинхронный сканер IP-адресов

## 🎯 Обзор

**Ping Scanner** - это мощный асинхронный инструмент для проверки доступности сетевых устройств по ICMP протоколу с измерением времени отклика. Скрипт предназначен для мониторинга и диагностики сетевой инфраструктуры.

## 📋 Основные возможности

- ✅ **Асинхронная проверка** - одновременная проверка сотен хостов
- ✅ **Измерение времени отклика** - средняя задержка за указанный период
- ✅ **Поддержка различных форматов ввода**:
    - Отдельные IP-адреса
    - CIDR диапазоны (IPv4/IPv6)
    - Простые диапазоны (start-end)
- ✅ **Множество форматов вывода** - текст, JSON, CSV
- ✅ **Детальное логирование** - настраиваемые уровни логирования
- ✅ **Отображение прогресса** - реальное время выполнения
- ✅ **Кросс-платформенность** - Windows, Linux, macOS
- ✅ **Конфигурация через файл** - все настройки в отдельном файле

## 📁 Структура проекта

```
ping_scanner/
├── __init__.py              # Пакетные определения
├── config.py               # Конфигурация и модели данных
├── scanner.py              # Основной сканер
├── ip_parser.py           # Парсер IP-адресов
├── reporter.py            # Генератор отчетов
├── utils.py               # Вспомогательные функции
├── main.py                # Точка входа
├── scanner_config.json    # Конфигурационный файл
└── ips.txt               # Файл с IP-адресами
```

## ⚙️ Установка

### Требования
- Python 3.9 или выше
- Доступ к команде `ping` в системе

### Установка зависимостей
Скрипт использует только стандартные библиотеки Python. Для работы с YAML конфигурациями (опционально):

```bash
pip install pyyaml
```

### Клонирование проекта
```bash
git clone <repository-url>
cd ping_scanner
```

## 🔧 Конфигурация

### Основной конфигурационный файл
Создайте файл `scanner_config.json` или используйте автоматически создаваемый по умолчанию:

```json
{
  "input_file": "ips.txt",
  "output_file": "report.txt",
  "log_file": "scanner.log",
  "timeout": 2.0,
  "ping_count": 1,
  "ping_duration": 60,
  "ping_interval": 1.0,
  "concurrent_limit": 100,
  "max_retries": 3,
  "exclude_self": true,
  "validate_ips": true,
  "report_format": "text",
  "log_level": "INFO",
  "show_progress": true,
  "progress_update_interval": 10,
  "save_raw_results": false,
  "raw_results_file": "raw_results.json"
}
```

### Параметры конфигурации

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `input_file` | string | "ips.txt" | Файл с IP-адресами для проверки |
| `output_file` | string | "report.txt" | Файл для сохранения отчета |
| `log_file` | string | "scanner.log" | Файл логов |
| `timeout` | float | 2.0 | Таймаут для одного ping (сек) |
| `ping_count` | int | 1 | Количество ping запросов для проверки доступности |
| `ping_duration` | int | 60 | Длительность измерения времени отклика (сек) |
| `ping_interval` | float | 1.0 | Интервал между ping запросами при измерении (сек) |
| `concurrent_limit` | int | 100 | Максимальное количество одновременных проверок |
| `max_retries` | int | 3 | Максимальное количество попыток при ошибках |
| `exclude_self` | bool | true | Исключать локальные адреса |
| `validate_ips` | bool | true | Валидировать IP-адреса |
| `report_format` | string | "text" | Формат отчета (text, json, csv) |
| `log_level` | string | "INFO" | Уровень логирования |
| `show_progress` | bool | true | Показывать прогресс выполнения |
| `progress_update_interval` | int | 10 | Интервал обновления прогресса (%) |
| `save_raw_results` | bool | false | Сохранять сырые результаты |
| `raw_results_file` | string | "raw_results.json" | Файл для сырых результатов |

### Формат файла с IP-адресами
Создайте файл `ips.txt` со следующим содержанием:

```txt
# Комментарии начинаются с #
# Отдельные адреса
8.8.8.8
8.8.4.4

# Диапазон CIDR
192.168.1.0/24

# Простой диапазон
10.0.0.1-10.0.0.10

# IPv6 адреса
2001:4860:4860::8888

# Список через запятую
192.168.0.1, 192.168.0.2, 192.168.0.3
```

## 🚀 Использование

### Базовый запуск
```bash
python main.py
```

### Предварительные шаги
1. Убедитесь, что команда `ping` доступна в системе
2. Создайте файл `ips.txt` с адресами для проверки
3. При необходимости отредактируйте `scanner_config.json`

### Логика работы
1. **Инициализация**:
    - Загрузка конфигурации из `scanner_config.json`
    - Настройка логирования
    - Проверка доступности команды `ping`

2. **Парсинг IP-адресов**:
    - Чтение файла `ips.txt`
    - Разбор различных форматов адресов
    - Фильтрация локальных адресов (если включено)
    - Сортировка адресов

3. **Сканирование (2 этапа)**:
    - **Этап 1: Проверка доступности**
        - Асинхронная проверка всех хостов
        - Ограничение одновременных запросов
        - Повторные попытки при ошибках
        - Отображение прогресса в реальном времени

    - **Этап 2: Измерение времени отклика**
        - Только для доступных хостов
        - Измерение в течение указанного времени (`ping_duration`)
        - Расчет средней задержки
        - Параллельное выполнение для всех хостов

4. **Формирование отчета**:
    - Генерация отчета в выбранном формате
    - Сохранение в файл
    - Вывод в консоль (для текстового формата)
    - Сохранение сырых данных (если включено)

## 📊 Форматы отчетов

### Текстовый формат (по умолчанию)
```
============================================================
ОТЧЕТ О СКАНИРОВАНИИ СЕТИ
Дата и время: 2024-01-15 10:30:00
============================================================

ОБЩАЯ СТАТИСТИКА:
  Всего хостов: 100
  Доступно: 85 (85.0%)
  Недоступно: 15 (15.0%)
  Средняя латентность: 24.56 мс
  Время сканирования: 125.3 сек

ДОСТУПНЫЕ ХОСТЫ (время отклика):
--------------------------------------------------
  8.8.8.8                                    12.34 мс
  8.8.4.4                                    15.67 мс
  ...

НЕДОСТУПНЫЕ ХОСТЫ:
--------------------------------------------------
  192.168.1.100
  192.168.1.101
  ...
```

### JSON формат
```json
{
  "metadata": {
    "generated_at": "2024-01-15T10:30:00",
    "config": {...},
    "scanner_version": "1.0.0"
  },
  "scan_results": {
    "summary": {
      "total": 100,
      "alive": 85,
      "dead": 15,
      "alive_percent": 85.0,
      "dead_percent": 15.0,
      "avg_latency_ms": 24.56,
      "scan_duration_seconds": 125.3
    },
    "alive_hosts": [...],
    "dead_hosts": [...],
    "latencies": {...}
  }
}
```

### CSV формат
```csv
IP Address,Status,Latency (ms),Timestamp
8.8.8.8,available,12.34,2024-01-15T10:30:00
8.8.4.4,available,15.67,2024-01-15T10:30:00
192.168.1.100,unavailable,,2024-01-15T10:30:00
```

## 🔍 Модули и их назначение

### 1. `config.py`
**Назначение**: Управление конфигурацией и моделями данных

**Ключевые классы**:
- `ScannerConfig`: Основной класс конфигурации с валидацией
- `PingResult`: Enum с возможными результатами ping
- `ScanResult`: Результат сканирования одного хоста
- `ScanSummary`: Сводная статистика по сканированию
- `ConfigLoader`: Загрузчик конфигурации из файла

**Особенности**:
- Использование `dataclass` для типобезопасности
- Автоматическая валидация значений
- Поддержка значений по умолчанию

### 2. `ip_parser.py`
**Назначение**: Парсинг IP-адресов из различных форматов

**Ключевые функции**:
- `parse_file()`: Чтение и парсинг файла с адресами
- `parse_line()`: Парсинг одной строки
- `filter_local_ips()`: Фильтрация локальных адресов
- `validate_ip()`: Валидация IP-адреса
- `sort_ips()`: Сортировка адресов

**Поддерживаемые форматы**:
- IPv4/IPv6 адреса
- CIDR нотация (192.168.1.0/24)
- Диапазоны (192.168.1.1-192.168.1.10)
- Списки через запятую

### 3. `scanner.py`
**Назначение**: Основная логика сканирования

**Ключевые классы**:
- `AsyncPingScanner`: Основной класс сканера
- `ProgressTracker`: Отслеживание и отображение прогресса

**Основные методы**:
- `scan_hosts()`: Главный метод сканирования
- `_check_availability()`: Проверка доступности хостов
- `_measure_latencies()`: Измерение времени отклика
- `_execute_ping()`: Выполнение одного ping запроса

**Особенности**:
- Асинхронное выполнение с семафорами
- Поддержка повторных попыток
- Кросс-платформенные команды ping

### 4. `reporter.py`
**Назначение**: Генерация и сохранение отчетов

**Ключевой класс**:
- `ReportGenerator`: Генератор отчетов в различных форматах

**Поддерживаемые форматы**:
- Текстовый (читаемый)
- JSON (машиночитаемый)
- CSV (для табличных редакторов)

### 5. `utils.py`
**Назначение**: Вспомогательные функции

**Основные функции**:
- `setup_logging()`: Настройка системы логирования
- `print_banner()`: Печать баннера при запуске
- `print_summary()`: Вывод сводки перед сканированием
- `validate_environment()`: Проверка окружения
- `get_input_file()`: Поиск файла с IP-адресами

### 6. `main.py`
**Назначение**: Точка входа, координация работы всех модулей

**Логика работы**:
1. Инициализация и проверка окружения
2. Загрузка конфигурации
3. Парсинг IP-адресов
4. Запуск сканирования
5. Генерация и сохранение отчета

## 🛠️ Расширенные сценарии использования

### Мониторинг сети
```bash
# Ежедневное сканирование с сохранением в JSON
python main.py

# Использование в cron для автоматического мониторинга
0 8 * * * cd /path/to/ping_scanner && python main.py
```

### Интеграция с другими системами
```python
# Пример использования как модуля
from ping_scanner import AsyncPingScanner, ConfigLoader

config = ConfigLoader.load("custom_config.json")
scanner = AsyncPingScanner(config)
results = await scanner.scan_hosts(["8.8.8.8", "8.8.4.4"])
```

### Массовое сканирование
```txt
# ips.txt - большой список адресов
10.0.0.0/16
192.168.0.0/24
172.16.0.0/20
```

## ⚠️ Ограничения и особенности

### Безопасность
- Требуются права на выполнение ICMP запросов
- На Windows может потребоваться запуск от имени администратора
- Ограничение количества одновременных запросов для предотвращения DDoS

### Производительность
- Рекомендуется `concurrent_limit` не более 500
- Большие диапазоны (/16 и выше) могут создавать нагрузку
- Использование таймаутов для предотвращения зависаний

### Сетевые особенности
- NAT и брандмауэры могут блокировать ICMP
- Некоторые провайдеры ограничивают ICMP трафик
- IPv6 может требовать дополнительной настройки

## 🔄 Обновление и поддержка

### Обновление конфигурации
При добавлении новых параметров в `ScannerConfig`:
1. Обновите `DEFAULT_CONFIG` в `ConfigLoader`
2. Обновите пример `scanner_config.json`
3. Добавьте обработку в `from_dict()` если необходимо

### Добавление нового формата отчета
1. Добавьте значение в `ReportFormat` enum
2. Реализуйте метод генерации в `ReportGenerator`
3. Добавьте в `generate()` метод

## 🐛 Отладка и логирование

### Уровни логирования
- `DEBUG`: Детальная информация для отладки
- `INFO`: Основная информация о процессе
- `WARNING`: Предупреждения
- `ERROR`: Критические ошибки

### Просмотр логов
```bash
# Просмотр логов в реальном времени
tail -f scanner.log

# Поиск ошибок
grep ERROR scanner.log

# Анализ производительности
grep "Сканирование завершено" scanner.log
```

## 📈 Производительность

### Оптимальные настройки
| Количество хостов | concurrent_limit | timeout | ping_duration |
|-------------------|------------------|---------|---------------|
| < 100             | 50               | 2.0     | 30            |
| 100-1000          | 100              | 2.0     | 60            |
| 1000-10000        | 200              | 1.5     | 30            |
| > 10000           | 300              | 1.0     | 15            |

### Оценка времени выполнения
```
Время = (Количество_хостов / concurrent_limit) * (timeout + ping_duration)
```

## 🤝 Вклад в разработку

### Отчет об ошибках
1. Проверьте логи `scanner.log`
2. Укажите версию Python и ОС
3. Приложите конфигурационный файл

### Предложения по улучшению
1. Создайте issue с описанием функционала
2. Обсудите реализацию
3. Создайте pull request


---

**Примечание**: Данный инструмент предназначен для легального мониторинга собственной инфраструктуры. Использование для сканирования чужих сетей без разрешения может нарушать законодательство.